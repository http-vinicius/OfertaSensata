// import { Theme } from '@mui/material';

const useStyles = () => ({
  root: {
    marginTop: '64px',
    border: '1px solid red',
  },
  box: {
    height: '100vh',
  },
});

export default useStyles;
